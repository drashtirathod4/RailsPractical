<% if flash[:errors] %>
    <% flash[:errors].each do |error| %>
        <p style = "color:red"><%= error %></p>
    <% end %>
<% end %>
  
<%= form_with(model: [@my_product, @my_order], local: true) do |form| %>
    <table cellpadding="15px">
        <tr>
            <td><%= form.label :my_prodcut_id %></td>
            <td><%= form.text_field :my_product_id %></td>
        </tr>
        <tr>
            <td><%= form.label :quantity %></td>
            <td><%= form.text_field :quantity %></td>
        </tr>
        <tr>
            <td><%= form.submit %></td>
        </tr>
        <tr>
            <td><b><%= link_to "Back", my_product_my_orders_path %></b></td>
        </tr>
    </table>
<% end %>
